export enum Trends{
    positive = "positive",
    negative = "negative",
}

export enum VoteTypes{
    positive,
    negative,
    undefined
}

export enum VoteText{
    now = "Vote Now",
    again = "Vote Again",
}